# Leave unset for local dev (the app auto-detects the current hostname). On Vercel, set this
# in the frontend project's Environment Variables to the deployed backend project's URL, e.g.
# https://nyayavault-api.vercel.app — no trailing slash.
VITE_API_BASE_URL=http://localhost:8000
